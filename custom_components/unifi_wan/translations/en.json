{
  "title": "UniFi WAN",
  "config": {
    "step": {
      "user": {
        "title": "UniFi WAN",
        "description": "Enter your UniFi OS connection details.",
        "data": {
          "host": "Host / IP",
          "api_key": "API Key",
          "site": "Site",
          "verify_ssl": "Verify SSL Certificate",
          "auto_speedtest": "Run Speedtest Automatically",
          "auto_speedtest_minutes": "Auto Speedtest Interval (minutes)"
        }
      },
      "reauth_confirm": {
        "title": "Re-authenticate UniFi WAN",
        "description": "The API key for {host} is no longer valid. Enter a new API key.",
        "data": {
          "api_key": "API Key"
        }
      },
      "reconfigure": {
        "title": "Reconfigure UniFi WAN",
        "description": "Change the address, API key or site this gateway is reached at. Entities and their history are kept. The speedtest schedule is under Configure.",
        "data": {
          "host": "Host / IP",
          "api_key": "API Key",
          "site": "Site",
          "verify_ssl": "Verify SSL Certificate"
        }
      }
    },
    "error": {
      "cannot_connect": "Cannot reach the console. Check the Host/IP (just the address, no https:// or port) and that TCP port 443 is open from Home Assistant to the console.",
      "invalid_auth": "The API key was rejected. Use a local API key created on the console under Settings > Control Plane > Integrations (requires UniFi Network 9.0+). Keys from unifi.ui.com are not supported.",
      "invalid_site": "The console returned 404 Not Found. Check the Site name (usually 'default', not the display name) and that the console runs UniFi OS (UDM/UDR/UXG/Cloud Key with firmware 2.x or later).",
      "ssl_error": "SSL certificate verification failed. Turn off 'Verify SSL Certificate' unless your console has a valid trusted certificate.",
      "timeout": "The console did not respond within 30 seconds. Check the Host/IP and that no firewall is blocking TCP port 443.",
      "unknown": "Unexpected error"
    },
    "abort": {
      "already_configured": "This UniFi controller and site is already configured.",
      "reauth_successful": "Re-authentication was successful.",
      "wrong_console": "That address answers with a different gateway from the one this entry was set up for. Reconfigure keeps this entry's entities, so it can only follow the same gateway to a new address or site. Add the other gateway as a separate integration entry.",
      "reconfigure_successful": "Reconfiguration was successful."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "UniFi WAN Options",
        "description": "Adjust performance and speedtest settings.",
        "data": {
          "host": "Host / IP",
          "api_key": "API Key (leave blank to keep current)",
          "site": "Site",
          "verify_ssl": "Verify SSL Certificate",
          "scan_interval": "Scan Interval (seconds)",
          "rate_interval_seconds": "Rate Interval (seconds, 0 to disable)",
          "auto_speedtest": "Run Speedtest Automatically",
          "auto_speedtest_minutes": "Auto Speedtest Interval (minutes)",
          "expected_download_mbps": "ISP Expected Download Speed (Mbit/s, 0 to disable)",
          "expected_upload_mbps": "ISP Expected Upload Speed (Mbit/s, 0 to disable)",
          "speed_tolerance_percent": "ISP Speed Tolerance (%)"
        },
        "data_description": {
          "expected_download_mbps": "What your line is sold as. The ISP Download Speed Status sensor compares each speedtest against it and reports Expected, Faster or Slower. Leave at 0 if you would rather not compare.",
          "expected_upload_mbps": "What your line is sold as. The ISP Upload Speed Status sensor compares each speedtest against it and reports Expected, Faster or Slower. Leave at 0 if you would rather not compare.",
          "speed_tolerance_percent": "How far either side of the expected speed still counts as meeting it. At 2%, a 500 Mbit/s line reads Expected between 490 and 510. Widen it for a connection that varies by time of day; 0 means only an exact match counts."
        }
      }
    },
    "error": {
      "cannot_connect": "Cannot reach the console. Check the Host/IP (just the address, no https:// or port) and that TCP port 443 is open from Home Assistant to the console.",
      "invalid_auth": "The API key was rejected. Use a local API key created on the console under Settings > Control Plane > Integrations (requires UniFi Network 9.0+). Keys from unifi.ui.com are not supported.",
      "invalid_site": "The console returned 404 Not Found. Check the Site name (usually 'default', not the display name) and that the console runs UniFi OS (UDM/UDR/UXG/Cloud Key with firmware 2.x or later).",
      "ssl_error": "SSL certificate verification failed. Turn off 'Verify SSL Certificate' unless your console has a valid trusted certificate.",
      "timeout": "The console did not respond within 30 seconds. Check the Host/IP and that no firewall is blocking TCP port 443.",
      "unknown": "Unexpected error",
      "already_configured": "Another UniFi WAN entry is already configured for that console and site."
    }
  },
  "services": {
    "run_speedtest": {
      "name": "Run Speedtest",
      "description": "Trigger a manual speedtest on the active UniFi Gateway immediately.",
      "fields": {
        "wan": {
          "name": "WAN interface",
          "description": "Optional WAN number to test (e.g. 1 or 2). Omit to test the active WAN."
        }
      }
    },
    "dump_raw_data": {
      "name": "Dump raw data",
      "description": "Write an unredacted JSON file of everything the controller returns, to config/unifi_wan_dumps/, for your own inspection. Contains IP addresses, MAC addresses, serial numbers and the ISP lookup - do not post it publicly; use Download diagnostics for that.",
      "fields": {
        "keep": {
          "name": "Files to keep",
          "description": "How many dumps to keep per gateway before the oldest is deleted."
        }
      }
    }
  },
  "exceptions": {
    "unknown_wan": {
      "message": "No configured UniFi gateway has a WAN{wan}. Leave the WAN field empty to test the active WAN."
    }
  }
}
