# The real-core suite in tests_ha/: config entries set up by Home Assistant's
# own config entry manager, flows through its flow manager and HTTP API, and
# entities added by real platforms. pytest-homeassistant-custom-component
# pins the Home Assistant release it was built against, so this file pins it
# rather than homeassistant itself.
pytest-homeassistant-custom-component==0.13.316
